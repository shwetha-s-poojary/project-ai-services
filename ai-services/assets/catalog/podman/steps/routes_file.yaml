routes:
  - pod: "ai-services--catalog"
    services:
      - containerPort: "8081/tcp"
        subdomain: "catalog-ui"

      - containerPort: "8080/tcp"
        subdomain: "catalog-api"

# Made with Bob
